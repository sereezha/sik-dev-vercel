---
import FormHeader from '@components/header/FormHeader.astro';
import GeneralLayout from './GeneralLayout.astro';
---

<GeneralLayout>
  <div class="content">
    <FormHeader />
    <slot />
  </div>
</GeneralLayout>

<style lang="scss">
  .content {
    display: grid;
    grid-template-rows: auto 1fr auto;
    gap: 44px;

    @media screen and (min-width: 768px) {
      gap: 40px;
    }
  }
</style>
